# --- KOMMUNIKATION ---
# MQTT-Bibliothek für den Empfang von Feedback und das Senden von Befehlen
paho-mqtt>=1.6.1

# --- HARDWARE & DISPLAY ---
# Installiert RPMock NUR auf Windows
Mock.GPIO; sys_platform == 'win32'

# Installiert RPi.GPIO NUR auf Linux (Raspberry Pi)
RPi.GPIO; sys_platform == 'linux'
rpi-lgpio; sys_platform == 'linux'

# Treiber für das 1602A QAPASS LCD im Parallel-Modus
RPLCD>=1.3.0

# --- ENTWICKLUNG & TESTING ---
# Zum sicheren Laden der lokalen IP des MQTT-Brokers aus der .env-Datei
python-dotenv>=1.0.1
